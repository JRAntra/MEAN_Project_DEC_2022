import { AutoHideAfterFiveDirective } from './auto-hide-after-five.directive';

describe('AutoHideAfterFiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoHideAfterFiveDirective();
    expect(directive).toBeTruthy();
  });
});
