import { FadeoutDirective } from './fadeout.directive';

describe('FadeoutDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeoutDirective();
    expect(directive).toBeTruthy();
  });
});
