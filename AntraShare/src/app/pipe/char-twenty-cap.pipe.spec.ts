import { CharTwentyCapPipe } from './char-twenty-cap.pipe';

describe('CharTwentyCapPipe', () => {
  it('create an instance', () => {
    const pipe = new CharTwentyCapPipe();
    expect(pipe).toBeTruthy();
  });
});
