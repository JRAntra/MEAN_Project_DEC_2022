import { DisappearDirective } from './disappear.directive';

describe('DisappearDirective', () => {
  it('should create an instance', () => {
    const directive = new DisappearDirective();
    expect(directive).toBeTruthy();
  });
});
