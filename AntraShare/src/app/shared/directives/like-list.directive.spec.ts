import { LikeListDirective } from './like-list.directive';

describe('LikeListDirective', () => {
  it('should create an instance', () => {
    const directive = new LikeListDirective();
    expect(directive).toBeTruthy();
  });
});
