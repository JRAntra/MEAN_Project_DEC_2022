import { HideAfterFiveSecDirective } from './hide-after-five-sec.directive';

describe('HideAfterFiveSecDirective', () => {
  it('should create an instance', () => {
    const directive = new HideAfterFiveSecDirective();
    expect(directive).toBeTruthy();
  });
});
